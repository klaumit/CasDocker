namespace DevForge
{
    internal static class Utils
    {
        public static void X2()
        {
            // var quit = new Quit("Hello, C# !");
            // dev.Send(quit);
        }
    }
}